"""
input: the test scores
initiate an iterator and an accumulator
loop through the list of test sores
add all the scores
increment counter by one
once the loop ends, there are no more scores
divide accumulator by the counter
display the output to the user
output: the class average
"""


"""
scores
interator,accumulator = 0
loop through scores
    accumulator = accumulator + scores
    iterator = iterator +1
output = accumulator / total score
print output
"""

